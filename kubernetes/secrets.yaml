apiVersion: v1
kind: Secret
metadata:
  name: saga-tracker-secrets
  namespace: ecommerce
type: Opaque
stringData:
  # k3d 기본값 - 실제 환경에서는 SealedSecrets 또는 External Secrets 사용
  db-url: "jdbc:postgresql://saga-tracker-db:5432/saga_tracker"
  db-username: "application"
  db-password: "application"
